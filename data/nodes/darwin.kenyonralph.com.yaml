---
# Configuration data for darwin.kenyonralph.com.

classes:
  - --rsyslog::client
  # TODO
  #- rsyslog::server
  - mysite::vms::linode
  - mysite::roles::dns_server

mysite::ntp::servers: "%{alias('ntp_servers')}"

...
